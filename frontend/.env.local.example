# Base URL of the MediVault backend API.
# Must include the /v1 prefix. Override this when the backend is not on localhost
# (e.g. a deployed environment) — the frontend calls it directly from the browser.
NEXT_PUBLIC_API_URL=http://localhost:8000/v1
